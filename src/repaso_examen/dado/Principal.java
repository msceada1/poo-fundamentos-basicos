package repaso_examen.dado;

public class Principal {

    /*
  Crea una clase Dado que pueda representar varios tipos de dados. Incluye un constructor por defecto que inicialice el valor en 1, un metodo lanzar() que genere un número aleatorio dependiendo de las caras, getters y setters para el valor actual, y un método que devuelva una representación como "Dado: X".​​

Luego, diseña una clase Jugador con atributos privados nombre (String) y Dado (Dado). Proporciona constructores (uno vacío y otro con nombre), métodos para lanzar el dado del jugador, obtener el valor actual del dado

Finalmente, crea una clase Partida que gestione múltiples jugadores:
⦁	Permita agregar jugadores y simular rondas donde todos los jugadores lanzan sus dados.
⦁	Lleve un registro del ganador de cada ronda y el total de rondas ganadas por jugador.
⦁	Proporcione un método para obtener el jugador con más victorias tras N rondas.
     */
}
